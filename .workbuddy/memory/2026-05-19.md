
## 2026-05-19 修复总结

### 问题根因（Digest 3810661343）
`src/app/result/page.tsx` 是 Server Component，但写入了 `onError={(e) => { ... }}` 客户端事件处理器，Next.js 15 直接抛出服务器异常。

### 修复内容
1. **新建 `src/components/StickerImage.tsx`**：Client Component，管理图片加载错误状态，显示改进的错误 UI（图标+文案+重试按钮）
2. **修复 `src/app/result/page.tsx`**：删除 onError 和重复定义的 STYLE_PROMPTS，使用 `<StickerImage />` 组件
3. **修复 `src/app/pricing/page.tsx`**：添加 `'use client'`，把 `<a href>` 改为 `<button onClick>`，实现 `handleUpgrade` 函数调用 POST `/api/checkout`
4. **修复 `src/app/settings/page.tsx`**：在 `handleUpgrade` 中检查 `res.ok`，失败时会 alert 错误提示（之前无反应是因为 data.url 为 undefined 时不提示）
5. **修复 `src/lib/supabase/admin.ts`**：Supabase Admin 改为惰性初始化，避免构建时失败
6. **修复 `src/app/api/creem/webhook/route.ts`**：添加 `supabaseAdmin` null 检查

### 构建状态
✅ `npm run build` 成功，所有页面编译通过

### 部署状态
✅ 已推送到 GitHub (commit: 4840ddb)，Vercel 自动部署中

### 待验证
1. 访问 https://aisticker.pics/result?p=cat&s=cute 确认图片加载正常
2. 登录后测试 settings 页面付费按钮是否正常跳转 Creem
3. 测试 pricing 页面两个付费按钮是否正常工作
